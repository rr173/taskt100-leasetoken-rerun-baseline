# Exclude the local vendor tree (the builder re-resolves deps from go.mod/go.sum
# so we don't ship a 100MB+ context), test artifacts, DB files and binaries.
vendor/
*.db
*.db-wal
*.db-shm
*.test
task100-leasetoken
.idea/
.vscode/
.DS_Store
